@php
    $notary = auth()->user();
    $notaryName = $notary?->name ?: 'Notaris';
    $shortName = collect(explode(' ', trim($notaryName)))->filter()->first() ?: 'Notaris';
    $initials = collect(explode(' ', trim($notaryName)))->filter()->take(2)->map(fn($p) => mb_substr($p, 0, 1))->implode('') ?: 'N';
    $statusLabels = ['submitted'=>'Diajukan','in_process'=>'Dalam Proses','waiting_document'=>'Menunggu Dokumen','completed'=>'Selesai','cancelled'=>'Dibatalkan'];
    $appointmentLabels = ['requested'=>'Menunggu ACC','approved'=>'Disetujui','rejected'=>'Ditolak','rescheduled'=>'Dijadwalkan Ulang','cancelled'=>'Dibatalkan'];
    $staffLabels = ['pending'=>'Menunggu ACC','active'=>'Aktif','blocked'=>'Diblokir','resigned'=>'Resign'];
    $formatDate = fn($date) => $date ? \Carbon\Carbon::parse($date)->translatedFormat('d F Y') : '-';
    $formatTime = fn($time) => $time ? \Carbon\Carbon::parse($time)->format('H:i') . ' WITA' : '-';
    $weekStart = now()->startOfWeek();
    $weekEnd = now()->endOfWeek();
    $approvedThisWeek = $appointments->filter(fn($a) => $a->status === 'approved' && \Carbon\Carbon::parse($a->appointment_date)->between($weekStart, $weekEnd));
    $dayKeys = ['senin','selasa','rabu','kamis','jumat','sabtu','minggu'];
@endphp
<!DOCTYPE html>
<html lang="id" data-admin-theme="light" data-admin-view="auto" data-super-admin="notaris">
<head>
    <meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}">
    <title>Dashboard Super Admin Notaris - Kantor Notaris/PPAT</title>
    <link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/admin-dashboard.css') }}?v={{ filemtime(public_path('css/admin-dashboard.css')) }}"><link rel="stylesheet" href="{{ asset('css/super-admin-dashboard.css') }}?v={{ filemtime(public_path('css/super-admin-dashboard.css')) }}">
</head>
<body>
<header class="admin-header"><div class="admin-brand"><div class="brand-logos"><img src="{{ asset('images/garuda_logo.png') }}" alt="Garuda"><img src="{{ asset('images/logo-ini-1.jpg') }}" alt="INI"><img src="{{ asset('images/PPAT.png') }}" alt="PPAT"></div><div><small>KANTOR NOTARIS/PPAT</small><strong>Dashboard Super Admin Notaris</strong></div></div><nav class="admin-top-links"><a href="{{ route('public.home') }}">Halaman Public</a><a href="#" data-open-section="attendance-recap">Rekap Absensi Staf</a></nav><div class="admin-actions"><button class="icon-btn" data-open-alerts><span>!</span>@if($alerts->count())<b>{{ $alerts->count() }}</b>@endif</button><button class="admin-profile" data-open-profile><span data-admin-initials>{{ $initials }}</span><div><strong data-admin-name>{{ $notaryName }}</strong><small>Notaris / Super Admin</small></div></button></div></header>
<div class="admin-layout"><aside class="admin-sidebar" id="adminSidebar"><div class="sidebar-person"><span data-admin-initials>{{ $initials }}</span><div><strong data-admin-name>{{ $notaryName }}</strong><small>NOT-{{ now()->format('Y') }}-{{ str_pad((string)$notary?->id, 3, '0', STR_PAD_LEFT) }} · Online</small></div></div><nav class="admin-nav" aria-label="Navigasi admin"><button class="active" data-section="overview"><img src="{{ asset('images/nav-icons/profil.svg') }}" alt=""><span>Dashboard</span></button><button data-section="transactions"><img src="{{ asset('images/nav-icons/berkas.svg') }}" alt=""><span>Transaksi</span>@if($stats['totalTransactions'])<b>{{ $stats['totalTransactions'] }}</b>@endif</button><button data-section="schedule"><img src="{{ asset('images/nav-icons/kontak.svg') }}" alt=""><span>Jadwal Temu</span>@if($stats['pendingSchedules'])<b>{{ $stats['pendingSchedules'] }}</b>@endif</button><button data-section="documents"><img src="{{ asset('images/nav-icons/pendaftaran.svg') }}" alt=""><span>Dokumen Kurang</span>@if($stats['waitingDocuments'])<b>{{ $stats['waitingDocuments'] }}</b>@endif</button><button data-section="delay"><img src="{{ asset('images/nav-icons/tracking.svg') }}" alt=""><span>Keterlambatan</span>@if($lateApplications->count())<b>{{ $lateApplications->count() }}</b>@endif</button><button data-section="staff"><img src="{{ asset('images/nav-icons/profil.svg') }}" alt=""><span>Staf</span>@if($stats['pendingStaff'])<b>{{ $stats['pendingStaff'] }}</b>@endif</button><button data-section="attendance-recap"><img src="{{ asset('images/dashboard-icons/attendance.svg') }}" alt=""><span>Absensi</span></button><button data-section="settings"><img src="{{ asset('images/nav-icons/layanan.svg') }}" alt=""><span>Pengaturan</span></button></nav><form method="POST" action="{{ route('logout') }}" class="logout-form">@csrf<button class="logout-btn" type="submit">Keluar</button></form></aside>
<main class="admin-main">
<section class="admin-section active" data-admin-section="overview"><div class="page-head"><div><p>PUSAT KONTROL NOTARIS</p><h1>Selamat datang, {{ $shortName }}</h1><small>Pantau seluruh operasional kantor, staf, transaksi, jadwal, absensi staf, dan keamanan akun utama notaris.</small></div><button class="primary" data-open-section="attendance-recap">Lihat Absensi Staf</button></div><div class="admin-stats"><article><i><img src="{{ asset('images/dashboard-icons/applications.svg') }}" alt=""></i><small>Total Transaksi</small><strong>{{ $stats['totalTransactions'] }}</strong><span>Dari database</span></article><article><i class="blue"><img src="{{ asset('images/dashboard-icons/process.svg') }}" alt=""></i><small>Dalam Proses</small><strong>{{ $stats['inProcess'] }}</strong><span>Berkas berjalan</span></article><article><i class="amber"><img src="{{ asset('images/dashboard-icons/calendar.svg') }}" alt=""></i><small>Jadwal Pending</small><strong>{{ $stats['pendingSchedules'] }}</strong><span>Perlu ACC</span></article><article><i class="red"><img src="{{ asset('images/dashboard-icons/late.svg') }}" alt=""></i><small>Terlambat</small><strong>{{ $lateApplications->count() }}</strong><span>Lewat 7 hari</span></article><article><i class="green"><img src="{{ asset('images/dashboard-icons/attendance.svg') }}" alt=""></i><small>Staf Hadir</small><strong>{{ $stats['staffPresentToday'] }}</strong><span>Hari ini</span></article></div><div class="overview-grid"><section class="panel"><header><div><h2>Transaksi Perlu Update</h2><span>Prioritas berkas yang belum bergerak</span></div><button data-open-section="transactions">Lihat semua</button></header><div class="mini-list">@forelse($lateApplications->take(5) as $application)<article><b>{{ $application->file_number }}</b><span>{{ $application->service_name ?? 'Layanan belum dipilih' }} · {{ $statusLabels[$application->status] ?? $application->status }}</span><em>Diperbarui {{ $application->updated_at ? \Carbon\Carbon::parse($application->updated_at)->diffForHumans() : '-' }}</em></article>@empty <div class="empty-state"><strong>Belum ada transaksi perlu update.</strong><p>Data akan muncul jika ada berkas yang belum bergerak.</p></div>@endforelse</div></section><section class="panel today-card"><h2>Kontrol Super Admin</h2><div class="attendance-state"><strong>Notaris tidak perlu absen</strong><span>Absensi hanya berlaku untuk admin dan staf. Notaris cukup memantau rekap kehadiran staf.</span></div><button class="primary" data-open-section="attendance-recap">Lihat Absensi Staf</button></section></div></section>
<section class="admin-section" data-admin-section="transactions"><div class="page-head"><div><p>SEMUA TRANSAKSI</p><h1>Monitoring & Update Progres</h1><small>Lihat seluruh transaksi dan ubah status progres berkas secara cepat.</small></div></div><div class="toolbar"><input type="search" placeholder="Cari nomor berkas, klien, atau layanan"><select><option>Semua Status</option><option>Diproses</option><option>Menunggu Dokumen</option><option>Terlambat</option><option>Selesai</option></select></div><div class="transaction-table"><div class="table-head"><span>Berkas</span><span>Klien</span><span>Layanan</span><span>Status</span><span>Tracking</span><span>Aksi</span></div>@forelse($applications as $application)<article><span><b>{{ $application->file_number }}</b><small>{{ $formatDate($application->submitted_at ?? $application->created_at) }}</small></span><span>{{ $application->client_name ?? '-' }}</span><span>{{ $application->service_name ?? 'Layanan belum dipilih' }}</span><span><mark class="{{ $application->status === 'waiting_document' ? 'amber' : ($application->status === 'in_process' ? 'blue' : '') }}">{{ $statusLabels[$application->status] ?? $application->status }}</mark></span><span>{{ $application->tracking_code }}</span><span><button data-update-progress="{{ $application->file_number }}">Update</button></span></article>@empty <div class="empty-state wide"><strong>Belum ada transaksi.</strong><p>Transaksi akan muncul setelah user membuat pengajuan.</p></div>@endforelse</div></section>
<section class="admin-section" data-admin-section="schedule"><div class="page-head"><div><p>PERSETUJUAN JADWAL</p><h1>ACC Jadwal Temu User</h1><small>Setujui jadwal atau berikan umpan balik saat waktu/hari sudah penuh.</small></div><button class="primary" type="button" data-open-manual-schedule>+ Tambah Jadwal Manual</button></div><section class="weekly-schedule-recap"><header><div><p>REKAP 1 MINGGU</p><h2>Jadwal Temu yang Sudah ACC</h2><span>Ringkasan jadwal terkonfirmasi {{ $formatDate($weekStart) }} - {{ $formatDate($weekEnd) }}.</span></div><strong data-weekly-acc-total>{{ $approvedThisWeek->count() }} ACC</strong></header><div class="weekly-days" role="tablist" aria-label="Filter jadwal mingguan">@foreach($dayKeys as $day)<button class="{{ $loop->first ? 'active' : '' }} {{ $approvedThisWeek->filter(fn($a)=>strtolower(\Carbon\Carbon::parse($a->appointment_date)->translatedFormat('l'))===$day)->isEmpty() ? 'quiet' : '' }}" type="button" data-schedule-day="{{ $day }}"><b>{{ ucfirst($day) }}</b><strong>{{ $approvedThisWeek->filter(fn($a)=>strtolower(\Carbon\Carbon::parse($a->appointment_date)->translatedFormat('l'))===$day)->count() }}</strong><span>Jadwal ACC</span></button>@endforeach</div><div class="approved-schedule-list">@forelse($approvedThisWeek as $appointment)<article data-schedule-day="{{ strtolower(\Carbon\Carbon::parse($appointment->appointment_date)->translatedFormat('l')) }}"><time>{{ $formatDate($appointment->appointment_date) }} · {{ $formatTime($appointment->appointment_time) }}</time><strong>{{ $appointment->title }}</strong><span>{{ $appointment->client_name ?? '-' }} · {{ ucfirst($appointment->mode) }}</span><button type="button" data-delete-schedule>Hapus</button></article>@empty <div class="empty-state wide"><strong>Belum ada jadwal ACC minggu ini.</strong><p>Jadwal yang disetujui akan tampil di sini.</p></div>@endforelse</div></section><div class="schedule-grid">@forelse($appointments->where('status','requested') as $appointment)<article><time>{{ $formatDate($appointment->appointment_date) }} ? {{ $formatTime($appointment->appointment_time) }}</time><h3>{{ $appointment->title }}</h3><p>{{ $appointment->client_name ?? '-' }} ? {{ ucfirst($appointment->mode) }}</p><span class="capacity">Menunggu ACC</span><footer><form method="POST" action="{{ route('notaris.appointments.approve', $appointment->id) }}">@csrf<button class="primary" type="submit">ACC Jadwal</button></form><form method="POST" action="{{ route('notaris.appointments.alternative', $appointment->id) }}">@csrf<input type="hidden" name="feedback" value="Slot jadwal penuh. Mohon pilih waktu alternatif."><button type="submit">Kirim Alternatif</button></form></footer></article>@empty <div class="empty-state wide"><strong>Belum ada jadwal pending.</strong><p>Permintaan jadwal user akan muncul di sini.</p></div>@endforelse</div></section>
<section class="admin-section" data-admin-section="documents"><div class="page-head"><div><p>DOKUMEN KURANG</p><h1>Permintaan Dokumen ke User</h1><small>Minta dokumen tambahan sesuai pengajuan dan isi catatan yang jelas.</small></div></div><div class="document-request"><form id="documentRequestForm"><label>Nomor Berkas<select name="file">@forelse($applications as $application)<option>{{ $application->file_number }} · {{ $application->service_name ?? 'Layanan' }}</option>@empty<option>Belum ada berkas</option>@endforelse</select></label><label>Dokumen yang Diminta<input name="document" placeholder="Contoh: SPPT PBB Tahun 2026"></label><label class="full">Catatan untuk User<textarea name="note" placeholder="Isi catatan dokumen yang kurang"></textarea></label><button class="primary" type="submit">Kirim Permintaan Dokumen</button></form><aside><h3>Dokumen Diminta</h3>@forelse($documentRequests as $document)<button type="button">{{ $document->file_number }} · {{ $document->document_name }}</button>@empty<p class="empty-state">Belum ada permintaan dokumen.</p>@endforelse</aside></div></section>
<section class="admin-section" data-admin-section="delay"><div class="page-head"><div><p>ALERT KETERLAMBATAN</p><h1>Notifikasi Proses Macet</h1><small>Sistem menandai berkas yang terlalu lama tidak berubah progres.</small></div></div><div class="delay-list">@forelse($lateApplications as $application)<article class="danger"><b>{{ $application->file_number }}</b><span>{{ $application->service_name ?? 'Layanan belum dipilih' }} belum bergerak sejak {{ $formatDate($application->updated_at) }}</span><button data-open-section="transactions">Update Sekarang</button></article>@empty <div class="empty-state wide"><strong>Belum ada keterlambatan.</strong><p>Berkas lewat SLA akan muncul di sini.</p></div>@endforelse</div></section>
<section class="admin-section" data-admin-section="staff"><div class="page-head"><div><p>MANAJEMEN STAF</p><h1>Persetujuan & Blokir Staf</h1><small>Notaris sebagai super admin dapat menyetujui pendaftaran staf baru dan memblokir staf yang sudah resign.</small></div></div><div class="super-staff-grid"><section class="panel super-staff-panel"><header><div><h2>Staf Baru Menunggu ACC</h2><span>Hanya notaris yang dapat mengaktifkan akun staf.</span></div></header><div class="staff-list">@forelse($staff->where('status','pending') as $person)<article><span>{{ mb_substr($person->name,0,1) }}</span><div><strong>{{ $person->name }}</strong><small>{{ $person->email }} · {{ ucfirst($person->role) }}</small></div><footer><form method="POST" action="{{ route('notaris.staff.approve', $person->id) }}">@csrf<button class="primary" type="submit">ACC Staf</button></form><form method="POST" action="{{ route('notaris.staff.reject', $person->id) }}">@csrf<button type="submit">Tolak</button></form></footer></article>@empty <div class="empty-state">Belum ada staf menunggu ACC.</div>@endforelse</div></section><section class="panel super-staff-panel"><header><div><h2>Staf Aktif & Resign</h2><span>Blokir akun staf yang sudah tidak bekerja agar akses aman.</span></div></header><div class="staff-list">@forelse($staff->where('status','!=','pending') as $person)<article class="{{ in_array($person->status,['blocked','resigned']) ? 'resigned' : '' }}"><span>{{ mb_substr($person->name,0,1) }}</span><div><strong>{{ $person->name }}</strong><small>{{ $staffLabels[$person->status] ?? $person->status }} · {{ $person->email }}</small></div><footer><form method="POST" action="{{ route('notaris.staff.block', $person->id) }}">@csrf<button type="submit">Block Resign</button></form></footer></article>@empty <div class="empty-state">Belum ada staf aktif/resign.</div>@endforelse</div></section></div></section>
<section class="admin-section" data-admin-section="attendance-recap"><div class="page-head"><div><p>REKAP ABSENSI STAF</p><h1>Absensi Staf Hari Ini</h1><small>Ringkasan kehadiran staf, terlambat, izin, dan belum absen.</small></div></div><div class="attendance-recap-grid"><article><strong>{{ $attendanceToday->where('status','present')->count() }}</strong><span>Hadir Hari Ini</span></article><article><strong>{{ $attendanceToday->where('status','late')->count() }}</strong><span>Terlambat</span></article><article><strong>{{ $attendanceToday->whereIn('status',['permission','sick'])->count() }}</strong><span>Izin/Sakit</span></article><article><strong>{{ max($stats['activeStaff'] - $attendanceToday->count(), 0) }}</strong><span>Belum Absen</span></article></div><div class="attendance-table"><div><b>Nama Staf</b><b>Tanggal</b><b>Jam Masuk</b><b>Status</b><b>Catatan</b><b>Update</b><b>Validasi</b></div>@forelse($attendanceToday as $attendance)<article><strong>Staf #{{ $attendance->user_id }}</strong><span>{{ $formatDate($attendance->attendance_date) }}</span><span>{{ $attendance->check_in_time ?: '-' }}</span><span>{{ ucfirst($attendance->status) }}</span><span>{{ $attendance->note ?: '-' }}</span><span>{{ $attendance->updated_at ? \Carbon\Carbon::parse($attendance->updated_at)->diffForHumans() : '-' }}</span><mark>DB</mark></article>@empty <div class="empty-state wide"><strong>Belum ada absensi staf hari ini.</strong><p>Data absensi akan muncul setelah staf melakukan absen.</p></div>@endforelse</div></section>
<section class="admin-section settings-overlay" data-admin-section="settings"><div class="settings-drawer"><header><div class="brand-logos"><img src="{{ asset('images/garuda_logo.png') }}" alt="Garuda"><img src="{{ asset('images/logo-ini-1.jpg') }}" alt="INI"><img src="{{ asset('images/PPAT.png') }}" alt="PPAT"></div><div><small>SUPER ADMIN NOTARIS</small><h1>Pengaturan</h1></div><button type="button" data-open-section="overview">&times;</button></header><div class="settings-profile"><span data-admin-initials>{{ $initials }}</span><div><strong data-admin-name>{{ $notaryName }}</strong><small>Notaris / Super Admin</small><button type="button" data-open-profile>Edit Profil Notaris</button></div></div><section><label>Bahasa</label><div class="choice-grid"><button class="active" data-lang="id">Bahasa Indonesia</button><button data-lang="en">English</button></div></section><section><label>Mode Tampilan</label><div class="choice-grid four"><button data-view="auto">Auto</button><button data-view="desktop">Desktop</button><button data-view="tablet">Tablet</button><button data-view="phone">Handphone</button></div></section><section><label>Tema</label><div class="choice-grid"><button class="active" data-theme="light">Siang</button><button data-theme="dark">Malam</button></div></section><section><label>Keamanan</label><button class="settings-row">Ubah Password <b>›</b></button><button class="settings-row danger">Logout Semua Perangkat <b>›</b></button></section></div></section>
</main></div>
@if(session('schedule_message'))
    <div class="dashboard-flash" data-dashboard-flash>{{ session('schedule_message') }}</div>
@endif
@if(session('staff_message'))
    <div class="dashboard-flash" data-dashboard-flash>{{ session('staff_message') }}</div>
@endif
<div class="admin-modal" id="manualScheduleModal"><div class="modal-card"><button class="modal-close" data-close-modal>&times;</button><p>JADWAL MANUAL</p><h2>Tambah Jadwal Temu</h2><form id="manualScheduleForm"><label>Nomor Berkas / Kode<input name="file" required></label><label>Nama Klien<input name="client" required></label><label>Agenda<input name="agenda" required></label><label>Tanggal<input type="date" name="date" required></label><label>Jam<input type="time" name="time" required></label><label>Mode Temu<select name="mode"><option>Offline kantor</option><option>Online meeting</option><option>Telepon/WhatsApp</option></select></label><label class="full">Catatan<textarea name="note"></textarea></label><button class="primary" type="submit">Simpan Jadwal Manual</button></form></div></div><div class="admin-modal" id="progressModal"><div class="modal-card"><button class="modal-close" data-close-modal>&times;</button><p>UPDATE PROGRES</p><h2 data-progress-title>Nomor Berkas</h2><form id="progressForm"><label>Status Baru<select name="status"><option>Validasi Berkas</option><option>Pengecekan Sertifikat</option><option>Validasi Pajak</option><option>Penandatanganan AJB</option><option>Balik Nama ke BPN</option><option>Selesai</option></select></label><label>Catatan Admin<textarea name="note"></textarea></label><button class="primary" type="submit">Simpan Update</button></form></div></div><div class="admin-modal" id="profileModal"><div class="modal-card"><button class="modal-close" data-close-modal>&times;</button><p>PROFIL NOTARIS</p><h2>Edit Profil Super Admin</h2><form id="adminProfileForm"><label>Nama Lengkap<input name="name" value="{{ $notaryName }}"></label><label>Jabatan<input name="role" value="Notaris / Super Admin"></label><label>Email Login<input type="email" name="email" value="{{ $notary?->email }}"></label><label>Nomor HP<input name="phone" value="{{ $notary?->phone }}"></label><button class="primary" type="submit">Simpan Profil</button></form></div></div><div class="admin-modal" id="alertsModal"><div class="modal-card alert-summary-modal"><button class="modal-close" data-close-modal>&times;</button><p>RANGKUMAN OPERASIONAL</p><h2>Daftar Perlu Tindakan</h2><div class="alert-summary-list active">@forelse($alerts as $alert)<article class="{{ $alert['type'] === 'late' ? 'danger' : 'warning' }}"><header><b>{{ $alert['title'] }}</b><span>{{ ucfirst($alert['type']) }}</span></header><strong>{{ $alert['description'] }}</strong><p>Data diambil dari database operasional.</p><footer><button data-open-section="{{ $alert['type'] === 'schedule' ? 'schedule' : ($alert['type'] === 'documents' ? 'documents' : 'transactions') }}">Buka Detail</button></footer></article>@empty <div class="empty-state wide"><strong>Tidak ada alert.</strong><p>Semua data operasional sedang aman.</p></div>@endforelse</div></div></div><div class="admin-modal" id="deviceLoginModal"><div class="modal-card device-login-card"><button class="modal-close" data-close-modal>&times;</button><img src="{{ asset('images/garuda_logo.png') }}" alt="Garuda"><p>KEAMANAN AKUN NOTARIS</p><h2>Akun notaris terdeteksi di perangkat lain</h2><span>Akun super admin hanya untuk 1 akun utama. Jika ingin login di perangkat ini, sesi perangkat lama akan ditandai keluar.</span><div class="device-actions"><button type="button" data-close-modal>Batal</button><button class="primary" type="button" data-confirm-device-login>Ya, login di sini</button></div></div></div><div class="admin-toast"></div><script>window.authAdminProfile = {!! json_encode(['name' => $notaryName, 'role' => 'Notaris / Super Admin', 'email' => $notary?->email, 'phone' => $notary?->phone], JSON_UNESCAPED_UNICODE) !!};</script><script src="{{ asset('js/admin-dashboard.js') }}?v={{ filemtime(public_path('js/admin-dashboard.js')) }}" defer></script><script src="{{ asset('js/super-admin-dashboard.js') }}?v={{ filemtime(public_path('js/super-admin-dashboard.js')) }}" defer></script>
</body></html>
